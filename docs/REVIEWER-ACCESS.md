# HINDSIGHT — Reviewer / Grader access

Everything a reviewer needs to verify the project — **most of it with no login at all.**

---

## 1. Verify with NO login (public artifacts)

| What | How |
|---|---|
| **Live intake form** (submit a real doc end-to-end) | https://reemmor.app.n8n.cloud/form/21593841-f8b8-43a2-88a8-8595ad3e2f39 — upload any `samples/*.md` or `.pdf`; a row appears in the Sheet + an email is sent |
| **Full Cloud workflow** (inspect every node + sticky note) | Import [`n8n/cloud/hindsight_cloud_workflow.json`](../n8n/cloud/hindsight_cloud_workflow.json) into any n8n (credentials stripped — re-bind your own to run) |
| **Self-hosted workflow** | [`n8n/hindsight_workflow.json`](../n8n/hindsight_workflow.json) (generated by `n8n/build_workflow.py`) |
| **Run the whole stack locally** | `docker compose up --build -d` → n8n `localhost:5678`, API `localhost:8000/docs` |
| **Committed screenshots** | `docs/screenshot-*.png` (workflow, FastAPI, dashboard, the 3 email types, sheet) |
| **Rendered email bodies** | [`docs/email-samples/*.html`](email-samples/) — open in a browser |

## 2. Live instances (owner login — shared privately, NOT in this public repo)

| Service | Identifier |
|---|---|
| n8n Cloud | `https://reemmor.app.n8n.cloud` |
| Main workflow | `aYEv22StywIPL3Rq` — *HINDSIGHT — Postmortem Intelligence (Cloud)* (Published/active) |
| Daily digest workflow | `L46dvnaJbKGvkCxH` — *HINDSIGHT — Daily Digest (Cloud)* (BON-2, cron 08:00 UTC) |
| Google Sheet registry | `1Z7tiPISHB5siYby_lQnWA9wtXbDXVSGTu4HGZ5Dk2tk` · tab `Incidents` (14 columns) |
| Supabase (BON-5) | project `zduaexkkhdnltyelvuwn` — `hindsight` |

> **Credentials are intentionally NOT committed** — this repo is public, and the assignment requires keys to live only in the n8n credential vault. To watch the live workflow editor / Gmail / Sheet, the owner shares their login directly with the grader (e.g. via the course LMS), or grants a read-only Sheet share. Everything functional is verifiable through §1 above without any secret.

## 3. Verification evidence (already captured)

- **Cloud audit** (`scripts/audit_n8n_cloud.py`): all required nodes present, Gemini `gemini-3-flash-preview`, retry 5×/3s, 14-column flatten, Gemini/Sheets/Gmail credentials bound, recent executions = success. Full report: [`docs/n8n-cloud-audit.json`](n8n-cloud-audit.json).
- **Tests**: 188 automated (FastAPI 67, extractor 7, node bodies 72 + prepare 7 + compose 8 + bonus 7 + self-hosted 20). `node`/`pytest` commands in the [README](../README.md#verification).
- **BON-5 Supabase semantic search — live‑verified:**

  ```text
  extension:  vector 0.8.0 (pgvector)        table: public.hindsight_incidents (RLS on)
  rows:       6 / 6 embedded (768-dim)        RPC: match_hindsight_incidents(query, count, threshold)
  index:      hindsight_incidents_embedding_idx (HNSW, vector_cosine_ops)

  semantic search ranked (query = the OpenSSL vuln-scan incident):
    1.0000  vulnerability-scan  vuln_scan_critical_openssl.md   (the query itself)
    0.1431  intrusion           credential-stuffing brute force
    -0.12   vulnerability-scan  (older vuln rows)
  ```

## 4. Requirement → evidence map

See [`docs/ASSIGNMENT-MAP.md`](ASSIGNMENT-MAP.md) (§1–§10) and [`docs/bonus-challenges.md`](bonus-challenges.md) (all 8 bonuses). Quick index:

| Course § | Where |
|---|---|
| §4 pipeline 1–6 | form/watch → `prepare.js`/extractor → Gemini → `enrich.js`/FastAPI → Sheets → `output_docs/` JSON+MD + Gmail |
| §5 Gemini | `gemini-3-flash-preview`, temp 0.2, JSON mode, `x-goog-api-key` credential, retry 5×/3s |
| §6 metadata API | `services/enrichment-api` — `/enrich /health /categories /sensitivity` (+ search/compare/digest) |
| §7.2 Sheet | 14 columns, `sheet_row.js` flatten, verified live |
| §8.2 Gmail | `[classification] New document processed: filename` + HTML body + Sheet link |
| §9 bonuses | BON-1 Vision · BON-2 digest · BON-3 dashboard · BON-4 retry · BON-5 pgvector · BON-6 Flash↔Pro · BON-7 ZIP batch · BON-8 confidential page |
